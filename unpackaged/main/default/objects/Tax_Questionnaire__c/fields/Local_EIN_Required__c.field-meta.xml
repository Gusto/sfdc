<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Local_EIN_Required__c</fullName>
    <inlineHelpText>If the answer to Local EIN Required is Yes, then the answer to Registration required is automatically Yes too since the employer needs to register in order to get their custom EIN.
If the agency uses the FEIN, need to confirm if we must prompt customers to register with the state/local? In most cases this will be Yes, but there are rare instances when the agency doesn’t require the customer to register, and when we file their first return it automatically creates their registration.</inlineHelpText>
    <label>Local EIN Required</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Picklist</type>
    <valueSet>
        <restricted>true</restricted>
        <valueSetDefinition>
            <sorted>false</sorted>
            <value>
                <fullName>Uses FEIN</fullName>
                <default>false</default>
                <label>Uses FEIN</label>
            </value>
            <value>
                <fullName>Uses State EIN</fullName>
                <default>false</default>
                <label>Uses State EIN</label>
            </value>
            <value>
                <fullName>Unique EIN format(see details)</fullName>
                <default>false</default>
                <label>Unique EIN format(see details)</label>
            </value>
        </valueSetDefinition>
    </valueSet>
</CustomField>
