<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Apply_to_Integration__c</fullName>
    <defaultValue>true</defaultValue>
    <description>This setting will apply unmasking only to the integration environment if checked. Else email ids for all mentioned users/profiles will be unmasked regardless of the environment.</description>
    <inlineHelpText>This setting will apply unmasking to intgn org if checked. Useful for checking this box for profiles.</inlineHelpText>
    <label>Apply to Integration?</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
